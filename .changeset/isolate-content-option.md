---
'@prosemirror-adapter/core': minor
---

Add an `isolateContent` node view option that makes the node view's `dom` non-editable and its `contentDOM` editable, so native browser editing cannot escape the `contentDOM`.
