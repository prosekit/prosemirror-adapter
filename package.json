{
  "name": "@prosemirror-adapter/monorepo",
  "type": "commonjs",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "description": "Monorepo for ProseMirror Adapter",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --sequential run build",
    "typecheck": "tsc -b",
    "commit": "git-cz",
    "ex": "pnpm --filter=e2e start",
    "test": "pnpm run build && pnpm --filter=e2e test",
    "test:install": "pnpm --filter=e2e test:install",
    "changeset": "changeset",
    "lint": "pnpm run lint:eslint && pnpm run lint:prettier && pnpm run lint:knip",
    "lint:eslint": "eslint --max-warnings 0",
    "lint:prettier": "prettier --check .",
    "lint:knip": "knip",
    "fix": "pnpm run fix:eslint && pnpm run fix:prettier && pnpm run fix:ts",
    "fix:eslint": "eslint --max-warnings 0 --fix",
    "fix:prettier": "prettier --write .",
    "fix:ts": "monorepo-typescript-references fix",
    "release": "changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@ocavue/eslint-config": "^4.11.0",
    "@ocavue/tsconfig": "^0.7.1",
    "@types/node": "^24.12.0",
    "eslint": "^10.6.0",
    "git-cz": "^4.9.0",
    "husky": "^9.1.7",
    "knip": "^6.24.0",
    "lint-staged": "^17.0.8",
    "monorepo-typescript-references": "^1.1.2",
    "pkg-pr-new": "^0.0.75",
    "prettier": "^3.9.4",
    "prettier-plugin-svelte": "^4.1.1",
    "typescript": "~6.0.3"
  }
}
